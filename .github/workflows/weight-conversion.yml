name: Weights Conversion Tests
on:
  pull_request:
    branches:
      - main
jobs:
  test-weight-conversions:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install torch tensorflow keras pytest timm tqdm
        pip install -e .
        
    - name: Find and run conversion scripts
      id: conversion
      run: |
        set -e  # Exit immediately if a command exits with a non-zero status
        
        TORCH_TO_KERAS_SCRIPTS=$(find kv/models -type f -name "convert_*_torch_to_keras.py")
        KERAS_TO_KERAS_SCRIPTS=$(find kv/models -type f -name "convert_*_keras_to_keras.py")
        CONVERSION_SCRIPTS="$TORCH_TO_KERAS_SCRIPTS $KERAS_TO_KERAS_SCRIPTS"
        
        echo "Found $(echo "$CONVERSION_SCRIPTS" | wc -l) conversion scripts to test"
        if [ -z "$CONVERSION_SCRIPTS" ]; then
          echo "⚠️ No conversion scripts found"
          exit 1
        fi
        
        for script in $CONVERSION_SCRIPTS; do
          echo "🔄 Running conversion script: $script"
          
          # Run the script and capture output
          OUTPUT=$(python "$script" 2>&1)
          echo "$OUTPUT"
          
          # Check for model equivalence test failure first
          if echo "$OUTPUT" | grep -q "Failed X"; then
            echo "❌ Failed: $script"
            echo "Stopping further execution due to failure"
            exit 1
          elif [ $? -ne 0 ]; then
            echo "❌ Failed: $script"
            echo "Stopping further execution due to failure"
            exit 1
          fi
          
          echo "✅ Successfully completed: $script"
          echo "----------------------------------------"
        done
        
        echo "🧹 Cleaning up generated weight files..."
        find . -name "*.keras" -type f -delete
        find . -name "*.h5" -type f -delete
        find . -name "*.weights" -type f -delete
        find . -name "*.pt" -type f -delete
        find . -name "*.safetensors" -type f -delete
        find . -name "*.pth" -type f -delete
        find . -name "*.png" -type f -delete
        
        echo "🎉 All conversions completed successfully!"
        
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: conversion-test-results
        path: |
          ./**/*_test_results.txt
          ./**/*_error.log
        if-no-files-found: ignore
        retention-days: 1
        
    - name: Cleanup workspace
      if: always()
      run: |
        echo "🧹 Performing final cleanup..."
        find . -name "__pycache__" -exec rm -rf {} +
        find . -name "*.pyc" -delete
        find . -name ".pytest_cache" -exec rm -rf {} +